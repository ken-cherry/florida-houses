import styled from "styled-components";

const Wrapper = styled.section`
  display: grid;
  place-items: center;
  margin: 5rem auto;

  .image-house {
    height: 20rem;
    width: 20rem;
  }
`;
export default Wrapper;
